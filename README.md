# vox

A new repository.

This project follows the shared agent-instructions baseline (writing voice, banned-term
dictionary, branch naming, and the pull-request → merge → branch-cleanup lifecycle). See
`AGENTS.md` for the rules every session starts from.
